# v0.14.0 — 2026-04-28

> Slave UI 冷蓝重构 · TLS 支持 · egui 双端 i18n · 端到端覆盖 10 场景 · 274 测试 / 0 失败
>
> Cool-blue slave redesign · TLS support · End-to-end i18n · 10 E2E scenarios · 274 tests / 0 failures

## 亮点 / Highlights

- 🎨 **Slave UI 整体重构** — 冷蓝 palette、shadcn 迁移、SidePanel 三段式 240px、状态栏脉动 ●、寄存器表格色彩语义化。
  *Slave UI redesigned: cool-blue palette, shadcn migration, three-section 240px sidebar, pulsing status dot.*
- 🔒 **Slave TLS 支持** — 新建对话框 TLS 选项,`Transport::TcpTls` 全链路打通,配置可持久化。
  *Slave gains TLS in the new-connection dialog with persisted config.*
- 🌐 **Master + Slave egui 中英双语** — 菜单、侧栏、状态栏、运行时错误串均接入 i18n,即时切换。
  *Both egui apps now support live language switching (zh/en).*
- 🎭 **Slave 空状态 Hero 动画** — 三色弦欢迎屏,首启不再空白。
  *New empty-state Hero: three-string animated welcome screen.*
- 🧪 **端到端覆盖 10 场景** — 异常码、生命周期、多设备、多 ScanGroup、主站扫描器、变异传播、并发读写,共 274 测试 / 0 失败。
  *E2E grows to 10 scenarios; workspace at 274 tests, 0 failures.*
- ♻️ **master-egui `update()` 减肥** — 460 → 190 行,抽 6 个 render 方法。
  *`master-egui::update()` shrunk from ~460 to ~190 lines.*

## 下载 / Downloads

由 CI 自动附加(下方资产):/ Attached automatically by CI below:

- **ModbusSlave / ModbusMaster** — Tauri 版(.dmg / .msi / .AppImage,带 WebView)
- **ModbusSlave-egui / ModbusMaster-egui** — 独立 egui 原生二进制(.tar.gz / .zip,无 WebView 依赖)
  - 文件名含 `-egui-` 后缀以区分。/ filenames carry the `-egui-` suffix.

## 完整变更 / Full Changelog

参见 [`CHANGELOG.md`](https://github.com/kelsoprotein-lab/ModbusSim/blob/main/CHANGELOG.md#0140---2026-04-28) /
See [`CHANGELOG.md`](https://github.com/kelsoprotein-lab/ModbusSim/blob/main/CHANGELOG.md#0140---2026-04-28).
