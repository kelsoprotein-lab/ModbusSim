## 1. 寄存器 Modal 组件

- [x] 1.1 创建 `RegisterModal.vue` 组件，支持 `mode: 'add' | 'edit'` prop，标题随模式变化
- [x] 1.2 在 Modal 中实现地址冲突检测：点击确认时遍历当前寄存器列表，若同类型同地址已存在则弹出"覆盖"/"取消"提示
- [x] 1.3 实现"显示高级选项"折叠/展开，切换数据类型、字节序、注释字段的显示
- [x] 1.4 样式 Modal，使用中文标签（名称、地址、类型、数据类型、字节序、注释）

## 2. 右键上下文菜单

- [x] 2.1 在 `SlaveView.vue` 中实现寄存器表格行的 `@contextmenu.prevent` 事件处理
- [x] 2.2 创建 `<div class="context-menu">` 绝对定位浮动菜单，包含"编辑寄存器"和"删除寄存器"两个选项
- [x] 2.3 点击菜单项"编辑寄存器"时以 `edit` 模式打开 Modal，预填当前行数据
- [x] 2.4 点击菜单项"删除寄存器"时调用 `remove_register` 后端命令并刷新列表
- [x] 2.5 在菜单外点击或按 ESC 时关闭菜单

## 3. 集成到 SlaveView

- [x] 3.1 在寄存器表格上方添加"添加寄存器"按钮（中文文案）
- [x] 3.2 点击按钮以 `add` 模式打开 Modal，清空所有字段
- [x] 3.3 Modal 中点击"保存"时调用 `add_register` 命令（add 模式）或 `remove_register` + `add_register` 序列（edit 模式），成功后刷新列表并关闭 Modal
- [x] 3.4 Modal 中点击"取消"或右上角×按钮时直接关闭 Modal，不做任何操作

## 4. 验证

- [ ] 4.1 手动测试：添加新寄存器 → 在表格中看到新增行
- [ ] 4.2 手动测试：右键编辑 → Modal 预填正确 → 修改保存后列表更新
- [ ] 4.3 手动测试：右键删除 → 行消失，无需确认
- [ ] 4.4 手动测试：添加重复地址 → 弹出覆盖确认 → 覆盖后值更新
- [ ] 4.5 手动测试：展开高级选项 → 填写数据类型/字节序 → 保存成功
